// Elifba'da matematik pratiği yok — Topic.tsx'in import edebilmesi için stub
import type { ContentTopic } from "@/data/types";

export function MathPractice(_props: { topic: ContentTopic }) {
  return null;
}
